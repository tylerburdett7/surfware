# Startup Video

Place your startup video here as **`startup.mov`** or **`startup.mp4`**.

- Recommended: ~8 seconds
- .mov (H.264) and .mp4 (H.264) both work in Electron
- Plays on both left and right screens when the app loads
- Video will fill each screen (object-fit: cover)
